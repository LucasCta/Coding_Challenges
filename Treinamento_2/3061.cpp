#include <bits/stdc++.h>
using namespace std;

void marcaMancha() { return; }

int main() {

  int N, M;
  cin >> N >> M;
  vector<vector<int>> S;
  vector<vector<int>> T;
  for (int i = 0; i < N; i++)
    for (int j = 0; j < M; j++)
      cin >> T[i][j];

  for (int i = 0; i < N; i++)
    for (int j = 0; j < M; j++)
      if (S[i][j])

        return 0;
}

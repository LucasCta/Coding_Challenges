#include <bits/stdc++.h>
using namespace std;
#define SIZE_VET 100 + 2

typedef struct {
  int x, y;
} Ponto;

typedef struct {
  Ponto a, b;
} Predio;

int main() {
  int t = 1;
  while (t--) {
    int N;
    cin >> N;

    int *vet = (int *)malloc(sizeof(int) * N);
    Ponto *pontos = (Ponto *)malloc(sizeof(Ponto) * N + 1);
  }
  return 0;
}
